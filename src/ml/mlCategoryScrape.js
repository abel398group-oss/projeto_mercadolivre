/**
 * Listagem Mercado Livre (API / sniffer) — fase seguinte.
 * Stub para completar a árvore `src/ml/`.
 */

export async function scrapeMlCategory() {
  throw new Error('[ml-category] não implementado nesta fase');
}
